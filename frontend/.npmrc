# Skip optional dependencies that require native compilation
optional=false

# Skip scripts for problematic packages
ignore-scripts=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
